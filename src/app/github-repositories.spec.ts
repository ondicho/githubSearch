import { GithubRepositories } from './github-repositories';

describe('GithubRepositories', () => {
  it('should create an instance', () => {
    expect(new GithubRepositories()).toBeTruthy();
  });
});
